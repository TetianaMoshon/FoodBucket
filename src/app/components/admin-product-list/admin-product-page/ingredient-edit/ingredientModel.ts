export class IngredientModel {
    // constructor(public ingName: string, public ingQuantity: number, public ingMeasure: string) {}
    constructor(public ingredientName: string, public quantity: number, public measure: string) {}
}

import { OrdersFiltersPipe } from './orders-filters.pipe';

describe('OrdersFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdersFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
